name: .NET CI

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore Chatbot/Chatbot.csproj

      - name: Build
        run: dotnet build Chatbot/Chatbot.csproj --no-restore --configuration Release

      - name: Run tests
        run: dotnet test Chatbot/Chatbot.csproj --no-build --configuration Release --verbosity normal